---
title: "Video generation"
openapi: "POST /video/generation"
---

Creates an async video generation job or direct asset depending on the upstream provider. The gateway attaches audit metadata and returns the provider payload verbatim when necessary.
