---
title: "Health"
description: "Call /healthz with the TypeScript SDK."
---

**Method**: `client.getHealth()`.

### Example
```typescript
const health = await client.getHealth();
```

### Returns

```json
{
  "status": "ok"
}
```
