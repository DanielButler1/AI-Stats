---
title: "Audio speech"
description: "Call /audio/speech with the Python SDK."
---

**Method**: `client.generate_speech()`.

### Example
```python
speech = client.generate_speech({
    "model": "openai/gpt-4o-mini-tts",
    "input": "Hello world",
    "voice": "alloy",
    "format": "mp3",
})
```

### Key parameters
- `model` (required): TTS-capable model id.
- `input` (required): Text to synthesize.
- `voice`: Voice preset (model-dependent, e.g., `alloy`).
- `format`: `mp3`, `wav`, `flac`, etc.
- `speed`: Playback speed multiplier (typically 0.25–4).

### Returns
Audio binary (Blob)
