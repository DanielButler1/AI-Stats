---
title: "Embeddings (Preview)"
description: "Preview SDK usage for Embeddings."
---

This SDK is in **preview**. Use the generated client for this endpoint.

Suggested call: Direct HTTP POST /embeddings

```cpp
// Instantiate the generated API client for C++
// Call Direct HTTP POST /embeddings with the request body matching the API reference.
```

Parameters and request/response shapes are identical to the REST API. Consult the API reference for the full field list.

