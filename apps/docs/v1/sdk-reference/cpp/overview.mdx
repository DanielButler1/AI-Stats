---
title: "C++ SDK (preview)"
description: "Generated client for the AI Stats Gateway. Models endpoint only for now."
---

- Status: Preview.
- Generate with `pnpm openapi:gen:cpp`.
- Wrapper `packages/sdk-cpp/index.cpp` exposes `getModels`.

See `packages/sdk-cpp/examples/models.cpp` for usage.

TypeScript and Python SDKs are production-ready; other languages follow soon.
